import styles from "./DefaultPageScreen.module.scss";

const DefaultPageScreen = () => {
  return (
    <div className={styles.DefaultPageScreen}>
      <h1>No boards yet.</h1>
    </div>
  );
};

export default DefaultPageScreen;
